function test() {
    let number = `Az sum Batman`
    for (let index = 0; index <= number.length; index++) {
        let currentDigit = number.charAt(index);
        console.log(currentDigit);
    }
}
test()